# 28F: full-line dispersion and physical-completeness audit

Run `python scripts/run_causality_audit.py` from the demo root, or this directory's `run.py` from any working directory. Use `--config`, `--input`, `--output`, or `--no-plots` as needed.

Read [CAUSALITY_AUDIT.md](../../CAUSALITY_AUDIT.md) before interpreting the plots. The analytic checker and mathematical Eq. 2 KK test pass; a complete real-field physical susceptibility has not been established. Signed photon energy, not wavelength, is the transform coordinate.
