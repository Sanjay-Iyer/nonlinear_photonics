# 28B — native cutoff and shell study

Run `python scripts/run_kspace_sweep.py`, then `python scripts/run_numerical_audit.py` (the latter also requires cached28C outputs). Configuration: `config/kspace_sweeps.json`, probe/tail settings in `config/numerical_audit.json`. Outputs: `outputs/28B_kspace_cutoff/`. All samples are native; no extension beyond this dataset. See28C for separately labeled extended raw data.
