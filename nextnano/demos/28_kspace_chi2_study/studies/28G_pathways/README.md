# 28G — pathways and resonance map

Run `python scripts/run_response_audit.py` from the Demo 28 directory. See
`../../RESPONSE_AUDIT.md`. All 16 terms come from the authoritative core and are
saved, while plots show only the top four or grouped sums. The resonance map
shows the motion of one/two-photon denominator minima with actual k samples.

Configuration: `../../config/response_audit.json`.
Outputs: `../../outputs/28G_pathways/`.
