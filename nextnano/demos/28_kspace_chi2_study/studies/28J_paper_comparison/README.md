# 28J — observable fit is not cutoff convergence

Run `python scripts/run_numerical_audit.py` after28A/B/C. Outputs: `outputs/28J_paper_comparison/`. Absolute observables use own-maximum normalization; signedRe/Im share one reference factor. Config: `config/numerical_audit.json`. Native and extended-grid fit tables are separate. Nearest-feature errors are diagnostics, not robust one-to-one assignments. Exact published observable remains unresolved.
