# 28A — regression anchor

Run `python studies/28A_baseline/run.py` or `python scripts/calculate_chi2.py` from Demo28. Configuration: `config/baseline.json`. Outputs: `outputs/28A_baseline/`. This rebuilds inputs from cached raw data and compares the complex spectrum with local historical fixtures. No licensed run or prior-demo import.
