# Demo 13 tables guide

Demo: `13_ax_bayesian_optimization_graded_acqw`  
Generated: 2026-07-31 (UTC)  
Generated by `report13.py`; edit that file, not this one.

Every table is a projection of the append-only trial ledger, so all of them
can be regenerated at any time without touching the optimization history.
Every table is CSV; the summary tables are also Markdown and JSON. Each CSV
has a `<name>.units.json` sidecar giving the unit of every column.

## Units used in this demo

| unit | meaning |
|---|---|
| `a.u. (relative |χ²|)` | Demo 11 Eq. 2 relative susceptibility: a lineshape and a trend with no absolute scale. **Never** pm/V. |
| `nm` | length or wavelength |
| `eV`, `meV` | energy |
| `probability` | integrated |ψ|² over a region, in [0, 1] |
| `overlap in [0,1]` | normalized envelope overlap used by state tracking |
| `0 or 1` | a boolean validity flag, in numeric form because Ax constrains on it |

## Validity criteria used for filtering

A trial is **valid** when it completed, passed every Demo 11 physical check,
returned the expected number of states, passed the origin-independence test,
and satisfied the configured bounds:

- `maximum_detuning_nm`: `15.0`
- `maximum_boundary_probability`: `0.001`
- `minimum_state_tracking_confidence`: `0.8`
- `maximum_orthonormality_error`: `0.001`
- `require_origin_independence`: `True`
- `require_expected_states`: `True`
- `require_physical_qc`: `True`

A trial that **failed mechanically** (solver crash, missing output) has no
objective at all and is reported to Ax through `mark_trial_failed`. A trial
that ran but is invalid keeps every metric it produced and is rejected by the
outcome constraints. A valid design with a near-zero response is neither: it
is a real observation, flagged `valid_low_response`.

---

## `bo_all_trials_parameters_and_outcomes.csv`

- one row = one Ax trial, with every input parameter, physical output, QC metric, objective, constraint, status and provenance path
- formats: CSV
- units: `tables/bo_all_trials_parameters_and_outcomes.units.json`
- intended use: the single source of truth for the run; every other table is derived from it

## `bo_trial_input_parameters.csv`

- one row = one Ax trial's canonical design parameters and the geometry they resolve to
- formats: CSV
- units: `tables/bo_trial_input_parameters.units.json`
- intended use: checking what geometry each trial actually simulated

## `bo_trial_nonlinear_optical_outputs.csv`

- one row = one Ax trial's chi(2) measures
- formats: CSV
- units: `tables/bo_trial_nonlinear_optical_outputs.units.json`
- intended use: comparing designs on χ² alone

## `bo_trial_electronic_structure_outputs.csv`

- one row = one Ax trial's energies, gaps, overlaps and matrix elements
- formats: CSV
- units: `tables/bo_trial_electronic_structure_outputs.units.json`
- intended use: diagnosing *why* a design's χ² changed

## `bo_trial_state_localization_and_tracking.csv`

- one row = one Ax trial's state-character probabilities and state-tracking result
- formats: CSV
- units: `tables/bo_trial_state_localization_and_tracking.units.json`
- intended use: checking that a good score is not a state-labelling artifact

## `bo_trial_quality_control_results.csv`

- one row = one Ax trial's numerical-quality and validity checks
- formats: CSV
- units: `tables/bo_trial_quality_control_results.units.json`
- intended use: auditing whether a result may be used at all

## `bo_best_objective_so_far_by_iteration.csv`

- one row = one BO iteration, with the best valid objective found up to and including it
- formats: CSV, Markdown, JSON
- units: `tables/bo_best_objective_so_far_by_iteration.units.json`
- intended use: deciding whether more BO iterations are worth licensed solver time

## `bo_top_ranked_valid_designs.csv`

- one row = one valid design, ranked by the active objective
- formats: CSV, Markdown, JSON
- units: `tables/bo_top_ranked_valid_designs.units.json`
- intended use: choosing candidates for Stage 5 validation; the `validated` column is False until that runs

## `bo_invalid_and_failed_trials.csv`

- one row = one rejected or failed trial, with the exact reason
- formats: CSV, Markdown, JSON
- units: `tables/bo_invalid_and_failed_trials.units.json`
- intended use: understanding what the search space is doing wrong

## `demo11_demo12_demo13_best_design_comparison.csv`

- one row = one reference or best design from Demos 11, 12 and 13, on identical columns
- formats: CSV, Markdown, JSON
- units: `tables/demo11_demo12_demo13_best_design_comparison.units.json`
- intended use: the final claim of the demo; read the QC columns before the χ² columns

## `bo_generated_candidates_by_iteration.csv`

- one row = one Ax proposal, with its lifecycle state (proposed, executed, completed, rejected, failed, duplicate)
- formats: CSV
- units: `tables/bo_generated_candidates_by_iteration.units.json`
- intended use: auditing the proposal lifecycle, including duplicates the deduplicator caught

## `bo_surrogate_predictions_selected_parameter_slices.csv`

- one row = one point of a surrogate slice, with predicted mean and standard error
- formats: CSV
- units: `tables/bo_surrogate_predictions_selected_parameter_slices.units.json`
- intended use: reproducing the surrogate figures exactly

## `bo_acquisition_values_for_proposed_candidates.csv`

- one row = one proposed candidate, with the acquisition value Ax reported for it
- formats: CSV
- units: `tables/bo_acquisition_values_for_proposed_candidates.units.json`
- intended use: seeing how much improvement Ax expected from each proposal

## `bo_parameter_importance.csv`

- one row = one parameter's first-order Sobol sensitivity for one modelled metric
- formats: CSV, Markdown, JSON
- units: `tables/bo_parameter_importance.units.json`
- intended use: ranking parameters for a follow-up study, with the model caveat attached

## `bo_pareto_optimal_designs.csv`

- one row = one nondominated design under the active multi-objective set
- formats: CSV, Markdown, JSON
- units: `tables/bo_pareto_optimal_designs.units.json`
- intended use: multi-objective selection without collapsing to one number

## `bo_top_designs_local_validation_results.csv`

- one row = one local-refinement, mesh, state-count or padding check on a top design
- formats: CSV
- units: `tables/bo_top_designs_local_validation_results.units.json`
- intended use: deciding whether an Ax optimum survives refinement

## `bo_top_designs_fabrication_robustness.csv`

- one row = one fabrication perturbation applied to a top design
- formats: CSV
- units: `tables/bo_top_designs_fabrication_robustness.units.json`
- intended use: deciding whether an optimum survives fabrication error

## `bo_random_grid_search_efficiency_comparison.csv`

- one row = one search method, with the evaluations it needed to reach the best known design
- formats: CSV, Markdown, JSON
- units: `tables/bo_random_grid_search_efficiency_comparison.units.json`
- intended use: justifying Bayesian optimization against cheaper baselines

## `bo_run_plan_and_case_counts.csv`

- one row = the planned and completed evaluation budget for this run
- formats: CSV, Markdown, JSON
- units: `tables/bo_run_plan_and_case_counts.units.json`
- intended use: confirming the YAML iteration count produced the intended budget

## `bo_demo12_warm_start_provenance.csv`

- one row = one candidate Demo 12 case, with its compatibility decision and whether Ax used it
- formats: CSV, Markdown, JSON
- units: `tables/bo_demo12_warm_start_provenance.units.json`
- intended use: auditing exactly which Demo 12 results entered the optimization, and which were rejected and why

## `bo_search_space_definition.csv`

- one row = one active search-space parameter
- formats: CSV, Markdown, JSON
- units: `tables/bo_search_space_definition.units.json`
- intended use: recording the exact search space of this run
