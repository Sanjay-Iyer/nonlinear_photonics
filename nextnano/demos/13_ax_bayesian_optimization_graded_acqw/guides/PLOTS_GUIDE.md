# Demo 13 plots guide

_Generated by `guides13.py` — edit that, not this file._

One entry per figure. Every figure has a CSV of exactly the numbers drawn, at the path given.

**Current result (experiment v3).** 23 proposals, 7 refused before the solver ran, 16 completed with no solver failures, **3 feasible**. The best feasible trial is **t0021** (asymmetry 0.387565, central barrier 0.85 nm, abrupt, relative chi2 at 1550 nm = 0.411027, detuning -13 nm). t0021 sits on the barrier **lower bound** and Stage 5 has not run, so it is the best design *found*, **not a validated optimum**.

**Units.** Every chi(2) number in this demo is a *relative* merit in arbitrary units (`a.u. (relative |chi2|)`). It is a lineshape and a trend with no absolute calibration. It is **never pm/V**, and values are only comparable between designs computed with the same mesh and settings.

## Historical corrections and state identity

The protected v3 ledger is never rewritten. It preserves the values originally
stored in `stored_*` columns, while every current table, ranking, and plot uses
the matching `corrected_*` value recomputed from peak/target wavelengths or the
heavy-hole energy array. If primary data are absent, the corrected value is
`unavailable`, never zero; `derived_value_status` and
`derived_value_provenance` say exactly what happened.

State energies carry one of four provenance labels: `solver`, `parsed historical output`,
`synthetic test`, or `unavailable`. Synthetic index energies are
permitted only by an explicit test opt-in and are never scientific evidence.
Stage 5 assigns every case directly to `state_tracking.anchor_case`, so shuffling
case order cannot change its reference. Near a crossing or avoided crossing,
energy order can swap while the physical envelope continues smoothly; labels
therefore follow overlap (with real energy continuity only as a secondary cost),
and a small assignment margin is reported as ambiguous rather than smoothed
away.

**Observed vs predicted.** Evaluated designs are drawn as markers and are never smoothed. Continuous surfaces are *surrogate predictions* with the evaluated points overlaid, so the two cannot be confused.

**A missing figure is a bug, not a licence problem.** A figure with no data becomes a labelled placeholder whose text says why.

---

## `bo_objective_value_by_trial.png`

**Objective of each evaluated trial.** Did later trials actually score better than earlier ones?

- **Data behind it:** `plot_data/bo_objective_value_by_trial.csv`
- **x-axis:** Trial number
- **y-axis:** Objective value (a.u.)
- **Colour / marker:** Marker shape is the grading profile.
- **Included:** the 16 trials that ran to completion (feasible or not)
- **Data type:** Observed — evaluated nextnano trials, drawn as markers, never smoothed.

**How to read it.** The initial Sobol trials scatter widely; if Bayesian optimization is working, the later trials cluster in the upper part of the range. Individual dips after the switch to the model are normal — the acquisition function deliberately spends some trials exploring.

**What it cannot tell you.** This is every completed trial including invalid ones; a high point here is not necessarily a usable design. Cross-check against the validity map and the QC table.

**In v3.** 16 points. Three sit above the feasibility line; the rest failed detuning. A high point is not a good design on its own.

## `bo_best_objective_so_far_by_iteration.png`

**Best valid objective per BO iteration.** How fast did the search improve, and has it stopped improving?

- **Data behind it:** `plot_data/bo_best_objective_so_far_by_iteration.csv`
- **x-axis:** BO iteration
- **y-axis:** Best objective value (a.u.)
- **Colour / marker:** Step line; only valid trials may raise it.
- **Included:** only the 10 completed model-based evaluations, numbered 1-10
- **Data type:** Observed — evaluated nextnano trials, drawn as markers, never smoothed.

**How to read it.** A monotone staircase. A long flat tail is the usual signal that further iterations are buying little, though it can also mean the model is stuck in a local region.

**What it cannot tell you.** Invalid trials never raise this curve, so it can sit below the maximum in the raw objective plot. That difference is the point, not an error.

**In v3.** Uses MBM iterations 1-10, not proposal attempts. The curve only steps at a feasible trial, so it moves at t0017 and t0021.

## `bo_chi2_at_1550_by_trial.png`

**Target-wavelength response per trial.** How strong is the response exactly at the telecom target?

- **Data behind it:** `plot_data/bo_chi2_at_1550_by_trial.csv`
- **x-axis:** Trial number
- **y-axis:** |χ²| at 1550 nm (a.u.)
- **Colour / marker:** Marker shape is the grading profile.
- **Included:** the 16 trials that ran to completion (feasible or not)
- **Data type:** Observed — evaluated nextnano trials, drawn as markers, never smoothed.

**How to read it.** In Mode A this is the objective itself. In Modes B and C it is a reported metric and may not improve monotonically.

**What it cannot tell you.** Relative units. A value at a fixed wavelength is not the structure's intrinsic strength — compare with the peak plot before concluding anything about the material response.

**In v3.** The objective itself. t0021 is highest among feasible trials at 0.411 relative units.

## `bo_peak_chi2_by_trial.png`

**Intrinsic peak response per trial.** How strong is each structure at its own resonance?

- **Data behind it:** `plot_data/bo_peak_chi2_by_trial.csv`
- **x-axis:** Trial number
- **y-axis:** Peak |χ²| (a.u.)
- **Colour / marker:** Marker shape is the grading profile.
- **Included:** the 16 trials that ran to completion (feasible or not)
- **Data type:** Observed — evaluated nextnano trials, drawn as markers, never smoothed.

**How to read it.** Read together with the resonance-wavelength plot: a large peak far from 1550 nm is a strong structure that is simply mistuned.

**What it cannot tell you.** The peak is taken over the configured focused window only; a resonance outside that window is invisible here.

**In v3.** Intrinsic strength regardless of where the peak sits. t0021 reaches 0.577, higher than its 1550 nm value, because its peak is at 1537 nm.

## `bo_resonance_wavelength_by_trial.png`

**Resonance wavelength per trial.** Where does each design put its resonance?

- **Data behind it:** `plot_data/bo_resonance_wavelength_by_trial.csv`
- **x-axis:** Trial number
- **y-axis:** Peak wavelength (nm)
- **Colour / marker:** Marker shape is the grading profile.
- **Included:** the 16 trials that ran to completion (feasible or not)
- **Data type:** Observed — evaluated nextnano trials, drawn as markers, never smoothed.

**How to read it.** Clustering toward 1550 nm over the run means the optimizer is tuning the resonance rather than only raising the amplitude.

**What it cannot tell you.** Discrete jumps can be a genuine change of which transition dominates, or a state-labelling change. Check the tracking-confidence plot at the same trial numbers.

**In v3.** Shows why most trials failed: the peak lands away from 1550 nm.

## `bo_detuning_from_1550_by_trial.png`

**Detuning from the target per trial.** How far off target is each design?

- **Data behind it:** `plot_data/bo_detuning_from_1550_by_trial.csv`
- **x-axis:** Trial number
- **y-axis:** Detuning from 1550 nm (nm)
- **Colour / marker:** Signed; the dashed line is zero detuning.
- **Included:** the 16 trials that ran to completion (feasible or not)
- **Data type:** Observed — evaluated nextnano trials, drawn as markers, never smoothed.

**How to read it.** The sign matters: a systematic offset means the geometry is pushing the transition energy consistently one way.

**What it cannot tell you.** The Ax constraint uses the absolute value, so points symmetric about zero are treated identically by the optimizer.

**In v3.** The binding constraint. t0021 is at -13 nm against a 15 nm bound; t0022 failed at -16 nm.

## `bo_asymmetry_vs_grading_thickness_objective.png`

**Sampling in asymmetry and grading.** Which part of the asymmetry–grading plane did the search visit, and what did it find?

- **Data behind it:** `plot_data/bo_asymmetry_vs_grading_thickness_objective.csv`
- **x-axis:** Quantum-well asymmetry, s
- **y-axis:** Grading thickness (nm)
- **Colour / marker:** Colour is the objective; marker shape is the grading profile.
- **Included:** the 16 trials that ran to completion (feasible or not)
- **Data type:** Observed — evaluated nextnano trials, drawn as markers, never smoothed.

**How to read it.** Dense colour-rich clusters are where the optimizer concentrated. Empty regions were judged unpromising by the model, not proven bad.

**What it cannot tell you.** A projection of a four-dimensional space: two points that look adjacent here can differ in barrier thickness or profile.

**In v3.** Grading axis is the REALIZED thickness. The 11 abrupt trials sit on the 0 nm line; only 5 points are genuinely graded.

## `bo_barrier_thickness_vs_grading_thickness_objective.png`

**Sampling in barrier and grading.** How do tunnelling and interface smoothing trade off?

- **Data behind it:** `plot_data/bo_barrier_thickness_vs_grading_thickness_objective.csv`
- **x-axis:** Central barrier thickness (nm)
- **y-axis:** Grading thickness (nm)
- **Colour / marker:** Colour is the objective; marker shape is the grading profile.
- **Included:** the 16 trials that ran to completion (feasible or not)
- **Data type:** Observed — evaluated nextnano trials, drawn as markers, never smoothed.

**How to read it.** Thin barriers hybridize the wells strongly; thick grades soften the same interfaces. This plane shows whether the two effects reinforce or cancel.

**What it cannot tell you.** Projection, as above. Also note that the geometric guard rejects grades wider than the narrowest adjacent layer, so the top-left corner is unreachable by construction.

**In v3.** Realized thickness again. Note the empty region below a 0.90 nm barrier: no grade fits there.

## `bo_asymmetry_vs_barrier_thickness_objective.png`

**Sampling in asymmetry and barrier.** Where is the response strongest in the two purely geometric parameters?

- **Data behind it:** `plot_data/bo_asymmetry_vs_barrier_thickness_objective.csv`
- **x-axis:** Quantum-well asymmetry, s
- **y-axis:** Central barrier thickness (nm)
- **Colour / marker:** Colour is the objective; marker shape is the grading profile.
- **Included:** the 16 trials that ran to completion (feasible or not)
- **Data type:** Observed — evaluated nextnano trials, drawn as markers, never smoothed.

**How to read it.** This is the plane closest to Demo 12's Stage 5 grid, so it is the fairest visual comparison against the grid search.

**What it cannot tell you.** Projection. Abrupt and heavily graded designs are superimposed here.

**In v3.** The clearest view of where the optimizer went: toward the thin barrier bound.

## `bo_grading_profile_objective_distribution.png`

**Objective distribution per profile.** Does the shape of the grade matter, beyond its thickness?

- **Data behind it:** `plot_data/bo_grading_profile_objective_distribution.csv`
- **x-axis:** Grading profile
- **y-axis:** Objective value (a.u.)
- **Colour / marker:** Box shows the quartiles; every evaluated trial is drawn as a jittered point.
- **Included:** the 16 trials that ran to completion (feasible or not)
- **Data type:** Observed — evaluated nextnano trials, drawn as markers, never smoothed.

**How to read it.** Overlapping boxes mean the profile shape is not resolved by the trials run so far.

**What it cannot tell you.** Bayesian optimization does not sample categories evenly — it deliberately spends more trials on the shape it currently believes in, so these groups have unequal and non-random sizes. This is not a controlled comparison; Demo 12 Stage 3 is.

**In v3.** CAUTION: erf 3, sigmoid 1, cosine 1, linear 0, and every graded trial was infeasible. This cannot rank profiles.

## `bo_parameter_sampling_by_iteration.png`

**Where each iteration sampled.** Did the search move as it learned?

- **Data behind it:** `plot_data/bo_parameter_sampling_by_iteration.csv`
- **x-axis:** Quantum-well asymmetry, s
- **y-axis:** Grading thickness (nm)
- **Colour / marker:** Colour is the BO iteration.
- **Included:** all 23 proposals, including the 7 refused before the solver
- **Data type:** Observed — evaluated nextnano trials, drawn as markers, never smoothed.

**How to read it.** Early (dark) points spread out; later (light) points concentrate. That contraction is the visual signature of exploitation.

**What it cannot tell you.** Includes pending and failed trials, which have parameters but no objective.

**In v3.** Includes refused proposals so the search history is complete. Refused points built nothing.

## `bo_surrogate_mean_asymmetry_vs_grading_thickness.png`

**Surrogate mean over asymmetry and grading.** What does the model believe the response is, across a plane it has not fully sampled?

- **Data behind it:** `plot_data/bo_surrogate_mean_asymmetry_vs_grading_thickness.csv`
- **x-axis:** Quantum-well asymmetry, s
- **y-axis:** Grading thickness (nm)
- **Colour / marker:** Colour is the predicted posterior mean; open circles are evaluated trials.
- **Included:** predicted points on a grid, not evaluated designs
- **Data type:** Surrogate-predicted — a continuous mesh from the fitted Ax model, with the evaluated points overlaid as open circles so the two cannot be confused.

**How to read it.** The mesh is a belief, not a measurement. Trust it near the evaluated points and treat it as a hypothesis far from them.

**What it cannot tell you.** Remaining parameters are held at the values recorded in bo.surrogate_slices.fixed_values in demo.yaml and repeated in the CSV. A different slice can look completely different.

**In v3.** Model belief, not measurement. 350 of 625 grid points are withheld because a grade there would be below 0.80 nm.

## `bo_surrogate_uncertainty_asymmetry_vs_grading_thickness.png`

**Surrogate uncertainty over asymmetry and grading.** Where is the model still unsure?

- **Data behind it:** `plot_data/bo_surrogate_uncertainty_asymmetry_vs_grading_thickness.csv`
- **x-axis:** Quantum-well asymmetry, s
- **y-axis:** Grading thickness (nm)
- **Colour / marker:** Colour is the predicted standard error; open circles are evaluated trials.
- **Included:** predicted points on a grid, not evaluated designs
- **Data type:** Surrogate-predicted — a continuous mesh from the fitted Ax model, with the evaluated points overlaid as open circles so the two cannot be confused.

**How to read it.** Uncertainty collapses around evaluated points and grows away from them. Bright regions are where another trial would teach the model the most.

**What it cannot tell you.** Standard error of the surrogate, not an experimental error bar. It says nothing about how accurate the underlying nextnano calculation is.

**In v3.** Small error bars in the sampled region are not calibrated confidence: there are only 16 observations.

## `bo_acquisition_function_asymmetry_vs_grading_thickness.png`

**Expected improvement over asymmetry and grading.** Where would an improvement-seeking rule sample next?

- **Data behind it:** `plot_data/bo_acquisition_function_asymmetry_vs_grading_thickness.csv`
- **x-axis:** Quantum-well asymmetry, s
- **y-axis:** Grading thickness (nm)
- **Colour / marker:** Colour is expected improvement; open circles are evaluated trials.
- **Included:** predicted points on a grid, not evaluated designs
- **Data type:** Surrogate-predicted — a continuous mesh from the fitted Ax model, with the evaluated points overlaid as open circles so the two cannot be confused.

**How to read it.** Peaks sit where the mean is high, the uncertainty is large, or both — that balance is what exploration versus exploitation means in practice.

**What it cannot tell you.** This is analytic expected improvement recomputed from Ax's posterior mean and standard error on the slice. Ax's own generation step optimizes a Monte-Carlo log-noisy-EI acquisition over the full space, so this surface is an interpretable stand-in and not a replay of Ax's internal decision.

**In v3.** Analytic EI on the objective ALONE. Its maximum can sit in infeasible territory. Read the constrained proxy in the CSV.

## `bo_surrogate_mean_barrier_vs_grading_thickness.png`

**Surrogate mean over barrier and grading.** What does the model believe about the barrier–grading plane?

- **Data behind it:** `plot_data/bo_surrogate_mean_barrier_vs_grading_thickness.csv`
- **x-axis:** Central barrier thickness (nm)
- **y-axis:** Grading thickness (nm)
- **Colour / marker:** Colour is the predicted posterior mean; open circles are evaluated trials.
- **Included:** predicted points on a grid, not evaluated designs
- **Data type:** Surrogate-predicted — a continuous mesh from the fitted Ax model, with the evaluated points overlaid as open circles so the two cannot be confused.

**How to read it.** As for the asymmetry–grading mean surface.

**What it cannot tell you.** Fixed values for the other parameters are recorded in the CSV and in demo.yaml.

**In v3.** 413 of 625 points are predictable; the rest cannot build a grade.

## `bo_surrogate_uncertainty_barrier_vs_grading_thickness.png`

**Surrogate uncertainty over barrier and grading.** Where is the model unsure in the barrier–grading plane?

- **Data behind it:** `plot_data/bo_surrogate_uncertainty_barrier_vs_grading_thickness.csv`
- **x-axis:** Central barrier thickness (nm)
- **y-axis:** Grading thickness (nm)
- **Colour / marker:** Colour is the predicted standard error; open circles are evaluated trials.
- **Included:** predicted points on a grid, not evaluated designs
- **Data type:** Surrogate-predicted — a continuous mesh from the fitted Ax model, with the evaluated points overlaid as open circles so the two cannot be confused.

**How to read it.** As for the asymmetry–grading uncertainty surface.

**What it cannot tell you.** Surrogate uncertainty only.

**In v3.** Brightest away from the 16 evaluated designs, as expected.

## `bo_peak_chi2_vs_detuning_pareto.png`

**Intrinsic strength against wavelength match.** Must a strong structure be mistuned?

- **Data behind it:** `plot_data/bo_peak_chi2_vs_detuning_pareto.csv`
- **x-axis:** |Detuning from 1550 nm| (nm)
- **y-axis:** Peak |χ²| (a.u.)
- **Colour / marker:** Colour is the response at 1550 nm.
- **Included:** the 16 trials that ran to completion (feasible or not)
- **Data type:** Observed — evaluated nextnano trials, drawn as markers, never smoothed.

**How to read it.** The upper-left corner is the ideal: strong and on target. Points forming a frontier from lower-left to upper-right are the real tradeoff.

**What it cannot tell you.** Nondominance here is over two objectives only; the tabulated Pareto set may use more.

**In v3.** The real tradeoff: the strongest designs are not the best-centred ones.

## `bo_chi2_1550_vs_peak_chi2.png`

**Target response against intrinsic peak.** Is the telecom response limited by strength or by tuning?

- **Data behind it:** `plot_data/bo_chi2_1550_vs_peak_chi2.csv`
- **x-axis:** Peak |χ²| (a.u.)
- **y-axis:** |χ²| at 1550 nm (a.u.)
- **Colour / marker:** Colour is the absolute detuning.
- **Included:** the 16 trials that ran to completion (feasible or not)
- **Data type:** Observed — evaluated nextnano trials, drawn as markers, never smoothed.

**How to read it.** Points near the diagonal are well tuned. Points far below it are strong structures wasting their strength at the wrong wavelength.

**What it cannot tell you.** Both axes are relative units on the same scale, so the diagonal is meaningful; neither is calibrated to pm/V.

**In v3.** Distance below the diagonal is detuning cost.

## `bo_chi2_1550_vs_boundary_probability.png`

**Target response against confinement quality.** Is the optimizer being rewarded for badly confined states?

- **Data behind it:** `plot_data/bo_chi2_1550_vs_boundary_probability.csv`
- **x-axis:** Boundary probability
- **y-axis:** |χ²| at 1550 nm (a.u.)
- **Colour / marker:** Marker shape is the grading profile.
- **Included:** the 16 trials that ran to completion (feasible or not)
- **Data type:** Observed — evaluated nextnano trials, drawn as markers, never smoothed.

**How to read it.** A rising trend to the right is the warning sign: it means the largest responses come from states leaking to the domain edge, which is a numerical artifact and not a design.

**What it cannot tell you.** The constraint bound is applied by Ax separately; this plot shows the raw relationship including rejected points.

**In v3.** Boundary probability is ~3e-5 everywhere, far inside its 1e-3 bound. This constraint never bound.

## `bo_chi2_1550_vs_state_tracking_confidence.png`

**Target response against tracking confidence.** Are the best-scoring designs the ones the tracker is least sure about?

- **Data behind it:** `plot_data/bo_chi2_1550_vs_state_tracking_confidence.csv`
- **x-axis:** State-tracking confidence
- **y-axis:** |χ²| at 1550 nm (a.u.)
- **Colour / marker:** Marker shape is the grading profile.
- **Included:** the 16 trials that ran to completion (feasible or not)
- **Data type:** Observed — evaluated nextnano trials, drawn as markers, never smoothed.

**How to read it.** High response at low confidence is the Demo 11 failure mode reappearing: an apparent gain produced by states swapping labels through an avoided crossing.

**What it cannot tell you.** Confidence is an overlap against the nearest evaluated neighbour, so it depends on how densely that region was sampled.

**In v3.** Confidence is high throughout. High confidence near the thin barrier still needs the Stage 5 anchor check.

## `bo_nonlinear_strength_vs_robustness_pareto.png`

**Strength against fabrication robustness.** Does the strongest design survive fabrication error?

- **Data behind it:** `plot_data/bo_nonlinear_strength_vs_robustness_pareto.csv`
- **x-axis:** Robustness score
- **y-axis:** |χ²| at 1550 nm (a.u.)
- **Colour / marker:** Colour is the peak response.
- **Included:** only the 3 completed trials that satisfied every constraint
- **Data type:** Observed — evaluated nextnano trials, drawn as markers, never smoothed.

**How to read it.** Upper-right is both strong and tolerant. A steep frontier means robustness is expensive.

**What it cannot tell you.** Robustness is only defined for designs that went through the Stage 5 perturbation study; other trials have no score and are absent.

**In v3.** Empty until Stage 5 produces robustness scores.

## `bo_parameter_importance.png`

**Surrogate sensitivity per parameter.** Which parameter does the fitted model think matters most?

- **Data behind it:** `plot_data/bo_parameter_importance.csv`
- **x-axis:** Parameter
- **y-axis:** First-order Sobol index
- **Colour / marker:** One bar per parameter.
- **Included:** predicted points on a grid, not evaluated designs
- **Data type:** Model-derived — a property of the fitted Ax surrogate, not of the physics.

**How to read it.** Larger magnitude means the surrogate's prediction changes more with that parameter over the search space.

**What it cannot tell you.** Only valid once a surrogate exists and only as good as its fit; Ax reports poor cross-validation for small trial counts. It is a sensitivity of a model, and it is not a claim about causal physical importance.

**In v3.** Sensitivity of the FITTED MODEL, not of the physics. With 16 observations, negative indices are estimator noise.

## `bo_partial_dependence_asymmetry.png`

**Surrogate dependence on asymmetry.** How does the model's prediction vary with asymmetry alone?

- **Data behind it:** `plot_data/bo_partial_dependence_asymmetry.csv`
- **x-axis:** Quantum-well asymmetry, s
- **y-axis:** Objective value (a.u.)
- **Colour / marker:** Line is the surrogate mean, band is ±1 standard error, dark points are evaluated trials.
- **Included:** predicted points on a grid, not evaluated designs
- **Data type:** Surrogate-predicted — a continuous mesh from the fitted Ax model, with the evaluated points overlaid as open circles so the two cannot be confused.

**How to read it.** Compare line and points: agreement means the one-dimensional summary is fair, disagreement means interactions dominate.

**What it cannot tell you.** Other parameters are held fixed at the recorded slice values — this is a slice, not a true marginal partial dependence averaged over the data.

**In v3.** A slice at fixed other coordinates, not a marginal average.

## `bo_partial_dependence_barrier_thickness.png`

**Surrogate dependence on barrier thickness.** How does the model's prediction vary with barrier thickness alone?

- **Data behind it:** `plot_data/bo_partial_dependence_barrier_thickness.csv`
- **x-axis:** Central barrier thickness (nm)
- **y-axis:** Objective value (a.u.)
- **Colour / marker:** Line is the surrogate mean, band is ±1 standard error, dark points are evaluated trials.
- **Included:** predicted points on a grid, not evaluated designs
- **Data type:** Surrogate-predicted — a continuous mesh from the fitted Ax model, with the evaluated points overlaid as open circles so the two cannot be confused.

**How to read it.** As above.

**What it cannot tell you.** Slice at fixed values, as above.

**In v3.** Rises toward the thin bound, which is why t0021 sits there.

## `bo_partial_dependence_grading_thickness.png`

**Surrogate dependence on grading thickness.** How does the model's prediction vary with grading thickness alone?

- **Data behind it:** `plot_data/bo_partial_dependence_grading_thickness.csv`
- **x-axis:** Grading thickness (nm)
- **y-axis:** Objective value (a.u.)
- **Colour / marker:** Line is the surrogate mean, band is ±1 standard error, dark points are evaluated trials.
- **Included:** predicted points on a grid, not evaluated designs
- **Data type:** Surrogate-predicted — a continuous mesh from the fitted Ax model, with the evaluated points overlaid as open circles so the two cannot be confused.

**How to read it.** As above. Under the hierarchical encoding this slice is the graded branch only; abrupt designs are a separate branch with no grading thickness at all.

**What it cannot tell you.** Slice at fixed values, as above.

**In v3.** The x-axis is the PROPOSED FRACTION (dimensionless), not nanometres. Realized nm is a separate column in the CSV.

## `bo_grading_profile_effect.png`

**Observed effect of grading profile.** Which grading shape produced the best objective on average?

- **Data behind it:** `plot_data/bo_grading_profile_effect.csv`
- **x-axis:** Grading profile
- **y-axis:** Mean objective value (a.u.)
- **Colour / marker:** One bar per profile; the CSV also carries the best and worst value and the trial count.
- **Included:** only trials whose realized grade exceeded the resolvable minimum
- **Data type:** Observed — evaluated nextnano trials, drawn as markers, never smoothed.

**How to read it.** Use the trial counts in the CSV before reading anything into a difference between bars.

**What it cannot tell you.** Unequal, non-random group sizes — see the caveat on the profile distribution plot.

**In v3.** 5 trials total across 3 profiles. Observations, never a ranking.

## `bo_valid_and_invalid_trials_by_iteration.png`

**Valid and invalid trials per iteration.** Is the optimizer wasting solver time in invalid regions?

- **Data behind it:** `plot_data/bo_valid_and_invalid_trials_by_iteration.csv`
- **x-axis:** BO iteration
- **y-axis:** Trials
- **Colour / marker:** Stacked bars: valid, invalid, failed.
- **Included:** the 16 trials that ran to completion (feasible or not)
- **Data type:** Observed — evaluated nextnano trials, drawn as markers, never smoothed.

**How to read it.** Healthy behaviour is a large invalid fraction early that shrinks as the constraint model learns the feasible region.

**What it cannot tell you.** Iteration 0 is the initial Sobol design, which has no model and is expected to be the worst-behaved.

**In v3.** 13 of 16 invalid. That is the headline result of v3.

## `bo_failure_reason_counts.png`

**Why trials were rejected or failed.** What actually rejects designs in this study?

- **Data behind it:** `plot_data/bo_failure_reason_counts.csv`
- **x-axis:** Rejection or failure reason
- **y-axis:** Trials
- **Colour / marker:** One bar per named reason; a trial with several violations is counted under each.
- **Included:** all 23 proposals, including the 7 refused before the solver
- **Data type:** Observed — evaluated nextnano trials, drawn as markers, never smoothed.

**How to read it.** A single dominant reason usually means a search bound should be reconsidered rather than a tolerance loosened.

**What it cannot tell you.** Mechanical failures are counted once as `mechanical_failure`; their detailed cause is in the invalid-and-failed table.

**In v3.** Two distinct groups: 7 refused before the solver, 13 completed but infeasible. They are not the same failure.

## `bo_boundary_probability_by_trial.png`

**Boundary probability per trial.** Are the states staying confined as the search moves?

- **Data behind it:** `plot_data/bo_boundary_probability_by_trial.csv`
- **x-axis:** Trial number
- **y-axis:** Boundary probability (log scale)
- **Colour / marker:** Dashed line is the configured constraint bound.
- **Included:** the 16 trials that ran to completion (feasible or not)
- **Data type:** Observed — evaluated nextnano trials, drawn as markers, never smoothed.

**How to read it.** Points climbing toward the bound mean the search is drifting into geometries the simulation domain cannot hold.

**What it cannot tell you.** Maximum over bound states; a single leaking state sets the value.

**In v3.** Nearly constant, which is why this constraint carries no information for the surrogate.

## `bo_state_tracking_confidence_by_trial.png`

**Tracking confidence per trial.** Can the tracker still follow the states the optimizer is asking about?

- **Data behind it:** `plot_data/bo_state_tracking_confidence_by_trial.csv`
- **x-axis:** Trial number
- **y-axis:** State-tracking confidence
- **Colour / marker:** Dashed line is the configured minimum.
- **Included:** the 16 trials that ran to completion (feasible or not)
- **Data type:** Observed — evaluated nextnano trials, drawn as markers, never smoothed.

**How to read it.** Dips coincide with avoided crossings or with jumps into unsampled regions.

**What it cannot tell you.** Each trial is compared to its nearest evaluated neighbour, so confidence is partly a measure of how isolated the trial was.

**In v3.** All above the 0.8 threshold.

## `bo_validity_map_asymmetry_vs_grading_thickness.png`

**Where valid designs live.** Where in the design space are the valid designs?

- **Data behind it:** `plot_data/bo_validity_map_asymmetry_vs_grading_thickness.csv`
- **x-axis:** Quantum-well asymmetry, s
- **y-axis:** Grading thickness (nm)
- **Colour / marker:** Blue circle valid, orange cross invalid, red square failed.
- **Included:** the 16 trials that ran to completion (feasible or not)
- **Data type:** Observed — evaluated nextnano trials, drawn as markers, never smoothed.

**How to read it.** Contiguous invalid regions are physical; scattered invalid points among valid ones usually mean a marginal tolerance.

**What it cannot tell you.** Projection of four dimensions onto two.

**In v3.** Where feasible designs live: a narrow sliver, only 3 points.

## `baseline_and_top_bo_designs_chi2_spectra.png`

**Baseline and top BO spectra.** How does the shape of the response change between the reference and the best designs?

- **Data behind it:** `plot_data/baseline_and_top_bo_designs_chi2_spectra.csv`
- **x-axis:** Fundamental wavelength (nm)
- **y-axis:** Normalized |χ²| (a.u.)
- **Colour / marker:** One line per design; dashed vertical line is 1550 nm.
- **Included:** reference results from Demo 11 or Demo 12, not Demo 13 trials
- **Data type:** Observed — evaluated nextnano trials, drawn as markers, never smoothed.

**How to read it.** Compare both the height and the position of the resonance; a better objective can come from either.

**What it cannot tell you.** Relative units, normalized for shape comparison. Never labelled pm/V.

**In v3.** Needs raw spectra; absent from the transferred bundle.

## `baseline_and_best_bo_composition_profiles.png`

**Baseline and best composition profiles.** What did the optimizer actually change about the structure?

- **Data behind it:** `plot_data/baseline_and_best_bo_composition_profiles.csv`
- **x-axis:** Position (nm)
- **y-axis:** Al fraction, x
- **Colour / marker:** One line per design.
- **Included:** reference results from Demo 11 or Demo 12, not Demo 13 trials
- **Data type:** Requested composition profile, as rendered into the nextnano input.

**How to read it.** The realized profile from the solver's own alloy output is validated separately per trial, exactly as in Demo 12 Stage 1.

**What it cannot tell you.** This is the requested profile. A licensed run must confirm the realized one before any conclusion depends on the grade shape.

**In v3.** Needs alloy-composition output, which was NOT transferred. This is the figure that would verify realized grading.

## `baseline_and_best_bo_conduction_band_edges.png`

**Baseline and best conduction band edges.** How does the electron potential differ between reference and best?

- **Data behind it:** `plot_data/baseline_and_best_bo_conduction_band_edges.csv`
- **x-axis:** Position (nm)
- **y-axis:** Energy (eV)
- **Colour / marker:** One line per design.
- **Included:** reference results from Demo 11 or Demo 12, not Demo 13 trials
- **Data type:** Solver output — requires a licensed run.

**How to read it.** Grading rounds the well corners; that rounding is the mechanism behind any change in the matrix elements.

**What it cannot tell you.** Empty without licensed nextnano output.

**In v3.** Needs raw solver output; not in the bundle.

## `baseline_and_best_bo_valence_band_edges.png`

**Baseline and best valence band edges.** How does the hole potential differ between reference and best?

- **Data behind it:** `plot_data/baseline_and_best_bo_valence_band_edges.csv`
- **x-axis:** Position (nm)
- **y-axis:** Energy (eV)
- **Colour / marker:** One line per design.
- **Included:** reference results from Demo 11 or Demo 12, not Demo 13 trials
- **Data type:** Solver output — requires a licensed run.

**How to read it.** Electron and hole potentials respond differently to the same grade, which is what shifts their centroid separation.

**What it cannot tell you.** Empty without licensed nextnano output.

**In v3.** Needs raw solver output; not in the bundle.

## `baseline_and_best_bo_electron_wavefunctions.png`

**Baseline and best electron envelopes.** Did the electron states redistribute between the wells?

- **Data behind it:** `plot_data/baseline_and_best_bo_electron_wavefunctions.csv`
- **x-axis:** Position (nm)
- **y-axis:** Envelope amplitude (a.u.)
- **Colour / marker:** One line per state per design.
- **Included:** reference results from Demo 11 or Demo 12, not Demo 13 trials
- **Data type:** Solver output — requires a licensed run.

**How to read it.** Amplitudes, not probability densities, and not offset onto their eigenenergies. The sign is physical here because the tracker aligns it along a branch.

**What it cannot tell you.** Empty without licensed nextnano output.

**In v3.** Needs raw solver output; not in the bundle.

## `baseline_and_best_bo_hole_wavefunctions.png`

**Baseline and best hole envelopes.** Did the hole states redistribute between the wells?

- **Data behind it:** `plot_data/baseline_and_best_bo_hole_wavefunctions.csv`
- **x-axis:** Position (nm)
- **y-axis:** Envelope amplitude (a.u.)
- **Colour / marker:** One line per state per design.
- **Included:** reference results from Demo 11 or Demo 12, not Demo 13 trials
- **Data type:** Solver output — requires a licensed run.

**How to read it.** Compare against the electron envelopes: χ² needs both, and cancellation between them is the known failure mode from the earlier replication work.

**What it cannot tell you.** Empty without licensed nextnano output.

**In v3.** Needs raw solver output; not in the bundle.

## `baseline_and_best_bo_state_localization.png`

**Baseline and best state localization.** Where does the ground state actually sit in each design?

- **Data behind it:** `plot_data/baseline_and_best_bo_state_localization.csv`
- **x-axis:** Region
- **y-axis:** Probability
- **Colour / marker:** Grouped bars, one group per region, one bar per design.
- **Included:** reference results from Demo 11 or Demo 12, not Demo 13 trials
- **Data type:** Observed — evaluated nextnano trials, drawn as markers, never smoothed.

**How to read it.** A large boundary bar invalidates the design regardless of its χ².

**What it cannot tell you.** Ground state only; per-state values are in the localization table.

**In v3.** Needs raw solver output; not in the bundle.

## `baseline_and_best_bo_transition_energies.png`

**Baseline and best transition energies.** Which transitions moved, and by how much?

- **Data behind it:** `plot_data/baseline_and_best_bo_transition_energies.csv`
- **x-axis:** Transition
- **y-axis:** Transition energy (eV)
- **Colour / marker:** Grouped bars, one bar per design.
- **Included:** reference results from Demo 11 or Demo 12, not Demo 13 trials
- **Data type:** Observed — evaluated nextnano trials, drawn as markers, never smoothed.

**How to read it.** The e1–hh1 energy sets the two-photon resonance; a change here is what retunes the spectrum.

**What it cannot tell you.** Tracked-label transitions, not raw energy order.

**In v3.** Needs raw solver output; not in the bundle.

## `top_bo_designs_chi2_spectra.png`

**Spectra of the top BO designs.** Do the best designs agree on a resonance, or are they different solutions?

- **Data behind it:** `plot_data/top_bo_designs_chi2_spectra.csv`
- **x-axis:** Fundamental wavelength (nm)
- **y-axis:** Normalized |χ²| (a.u.)
- **Colour / marker:** One line per top design.
- **Included:** only the 3 completed trials that satisfied every constraint
- **Data type:** Observed — evaluated nextnano trials, drawn as markers, never smoothed.

**How to read it.** Several distinct resonance positions with similar objectives means the problem has multiple optima, and the ranking between them is not robust.

**What it cannot tell you.** Relative units.

**In v3.** Needs raw spectra; not in the bundle.

## `bo_vs_random_search_best_objective.png`

**BO against random search.** Did Bayesian optimization beat random sampling at equal cost?

- **Data behind it:** `plot_data/bo_vs_random_search_best_objective.csv`
- **x-axis:** Evaluations
- **y-axis:** Best objective value (a.u.)
- **Colour / marker:** One step line per method.
- **Included:** the 16 trials that ran to completion (feasible or not)
- **Data type:** Observed — evaluated nextnano trials, drawn as markers, never smoothed.

**How to read it.** Compare at equal evaluation counts, not at the end of the run. The area between the curves is the solver time saved.

**What it cannot tell you.** A single random seed is a single sample. Rerun with several seeds before making a quantitative efficiency claim.

**In v3.** Only meaningful after a Demo 12 replay; not run for v3.

## `bo_vs_grid_search_best_objective.png`

**BO against grid search.** Did Bayesian optimization beat the Demo 12 grid at equal cost?

- **Data behind it:** `plot_data/bo_vs_grid_search_best_objective.csv`
- **x-axis:** Evaluations
- **y-axis:** Best objective value (a.u.)
- **Colour / marker:** One step line per method.
- **Included:** the 16 trials that ran to completion (feasible or not)
- **Data type:** Observed — evaluated nextnano trials, drawn as markers, never smoothed.

**How to read it.** The grid curve rises in its own traversal order, which is a real property of how a grid is actually run.

**What it cannot tell you.** The grid's ordering is a design choice; a different traversal gives a different curve. The end point is order-independent, the path is not.

**In v3.** As above.

## `bo_random_grid_evaluations_to_best_known_result.png`

**Evaluations to the best known design.** How many nextnano runs did each method need?

- **Data behind it:** `plot_data/bo_random_grid_evaluations_to_best_known_result.csv`
- **x-axis:** Search method
- **y-axis:** Evaluations
- **Colour / marker:** One bar per method; zero means the threshold was never reached.
- **Included:** the 16 trials that ran to completion (feasible or not)
- **Data type:** Observed — evaluated nextnano trials, drawn as markers, never smoothed.

**How to read it.** This is the number that decides whether Bayesian optimization pays for itself in licensed solver time.

**What it cannot tell you.** Threshold is a configured fraction of the best known value in the replay pool, and the pool bounds what any method can find.

**In v3.** As above.

## `demo11_demo12_demo13_best_chi2_spectra.png`

**Demo 11, 12 and 13 best spectra.** Did the optimization improve on the earlier demos?

- **Data behind it:** `plot_data/demo11_demo12_demo13_best_chi2_spectra.csv`
- **x-axis:** Fundamental wavelength (nm)
- **y-axis:** Normalized |χ²| (a.u.)
- **Colour / marker:** One line per demo's best design; dashed vertical line is 1550 nm.
- **Included:** reference results from Demo 11 or Demo 12, not Demo 13 trials
- **Data type:** Observed — evaluated nextnano trials, drawn as markers, never smoothed.

**How to read it.** This is the headline comparison of the whole demo. Read it with the comparison table, which carries the QC status of each design.

**What it cannot tell you.** All three curves must come from the same physics settings to be comparable; the comparison table records whether they do.

**In v3.** Cross-demo comparison. v2 and v3 numbers are NOT comparable: different mesh and different search space.

## `paper_figure2d_simulation_comparison_full_range.png`

**Paper comparison, full range.** Does the simulated lineshape resemble the published one?

- **Data behind it:** `plot_data/paper_figure2d_simulation_comparison_full_range.csv`
- **x-axis:** Fundamental wavelength (nm)
- **y-axis:** Normalized |χ²| (a.u.)
- **Colour / marker:** One line per source, including the digitized paper curve.
- **Included:** reference results from Demo 11 or Demo 12, not Demo 13 trials
- **Data type:** Mixed — simulated curves are computed; the paper curve is eye-digitized.

**How to read it.** Compare the position and relative shape of features only.

**What it cannot tell you.** The digitized curve carries an unquantified reading error and is a visual reference, never a target or a validation ground truth. See PAPER_COMPARISON_GUIDE.md.

**In v3.** Normalized shape comparison only. Never a calibration.

## `paper_figure2d_simulation_comparison_telecom_range.png`

**Paper comparison, telecom range.** Does the agreement hold in the telecom band specifically?

- **Data behind it:** `plot_data/paper_figure2d_simulation_comparison_telecom_range.csv`
- **x-axis:** Fundamental wavelength (nm)
- **y-axis:** Normalized |χ²| (a.u.)
- **Colour / marker:** As above, restricted to the telecom window.
- **Included:** reference results from Demo 11 or Demo 12, not Demo 13 trials
- **Data type:** Mixed — as above.

**How to read it.** This is the window the optimization actually targets.

**What it cannot tell you.** As above.

**In v3.** As above, zoomed to the telecom band.

## `paper_measured_sh_intensity_comparison.png`

**Measured SH intensity, plotted separately.** How does the measured second-harmonic intensity behave?

- **Data behind it:** `plot_data/paper_measured_sh_intensity_comparison.csv`
- **x-axis:** Fundamental wavelength (nm)
- **y-axis:** Measured SH intensity (a.u.)
- **Colour / marker:** One line per measured dataset.
- **Included:** reference results from Demo 11 or Demo 12, not Demo 13 trials
- **Data type:** Measured, if any measured dataset is configured.

**How to read it.** Kept on its own axes deliberately: measured SH intensity depends on the pump, the geometry and the collection efficiency, and is not proportional to a simulated χ².

**What it cannot tell you.** Empty unless paper_comparison.measured_intensity_csv is configured. Never plotted on the same axis as a simulated susceptibility.

**In v3.** Digitized from a figure. Visual reference only, never a target.

